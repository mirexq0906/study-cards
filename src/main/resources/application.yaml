spring:
  application:
    name: StudyCards
  datasource:
    url: jdbc:h2:mem:studycards
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
